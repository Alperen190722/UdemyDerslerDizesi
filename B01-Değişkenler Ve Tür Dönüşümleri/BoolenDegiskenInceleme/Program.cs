namespace BoolenDegiskenInceleme
{
    internal class Program
    {
        static void Main(string[] args)
        {
            bool kullaniciAktif = true; // Evet
            bool urunAktif = false; // Hayır (Olumsuzluk)

            Console.WriteLine(kullaniciAktif);
            Console.WriteLine(urunAktif);
        }
    }
}
